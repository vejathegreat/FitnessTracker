package com.velaphi.fitnesstracker

import org.junit.runner.RunWith
import org.junit.runners.Suite

@RunWith(Suite::class)
@Suite.SuiteClasses(
    // Core module tests
    // Note: These would need to be imported if they exist
    // CoreTestSuite::class,
    
    // MealPlan module tests
    // Note: These would need to be imported if they exist
    // MealPlanTestSuite::class,
    
    // Other module tests would go here
)
class FitnessTrackerTestSuite {
    // This class serves as a comprehensive test suite to run all FitnessTracker tests together
    // Currently serves as a placeholder for when cross-module testing is implemented
}
